The CPU only support the required instructions, if other instructions is input, the ALU unit will do nothing

The code will initially choose the intruction.bin as the input, if you want to testing as file
please change the read file name in file: InstructionRAM.v

All data in Main memory are displayed on the terminal, 10 data per line